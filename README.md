# 📊 GitHub Release Downloads Tracker

Автоматическое отслеживание и визуализация скачиваний релизов GitHub с красивыми SVG графиками.

## Возможности

- ⏰ Ежедневное автоматическое обновление через GitHub Actions
- 📈 Красивые SVG графики в стиле star-history.com
- 🌙 Тёмная тема с градиентами и glow-эффектами
- 📊 Отслеживание нескольких репозиториев одновременно
- 📦 Группировка статистики по форматам файлов (.exe, .dmg, .zip и т.д.)
- 🔰 Бейджи для README
- 📄 GitHub Pages с интерактивными графиками
- 🚀 Пагинация API — поддержка репозиториев с любым количеством релизов

## Быстрый старт

### 1. Настройка репозитория

1. Склонируйте или создайте новый репозиторий
2. Скопируйте все файлы из этого проекта
3. Отредактируйте `config.json`:

```json
{
  "repos": [
    "loli669/SoundCloud-Desktop"
  ]
}
```

Вы можете добавить несколько репозиториев для одновременного отслеживания.

### 2. Включите GitHub Pages

1. Перейдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: main → /docs
4. Сохраните изменения

### 3. Запустите Action

1. Перейдите в Actions
2. Выберите "Track Downloads"
3. Нажмите "Run workflow"

Через несколько минут данные будут собраны, графики сгенерированы и опубликованы на GitHub Pages!

## Использование

### Локальный запуск

```bash
# Собрать статистику
npm run fetch

# Сгенерировать графики
npm run chart

# Выполнить оба шага
npm run update
```

**GITHUB_TOKEN не обязателен**, но рекомендуется для избежания rate limit:
- Без токена: 60 запросов/час
- С токеном: 5000 запросов/час

### Добавление бейджа в README

```markdown
![Downloads](https://loli669.github.io/download-history/loli669_SoundCloud-Desktop.svg)
```

**Живой пример**: https://loli669.github.io/download-history/

## Структура проекта

```
.
├── config.json                        # Конфигурация отслеживаемых репозиториев
├── .github/workflows/
│   └── track-downloads.yml            # GitHub Action
├── scripts/
│   ├── fetch-stats.js                 # Сбор статистики из GitHub API
│   └── generate-chart.js              # Генерация SVG графиков
├── data/
│   └── owner_repo.json                # Исторические данные
└── docs/
    ├── index.html                     # GitHub Pages
    └── owner_repo.svg                 # Графики
```

## Формат данных

Данные хранятся в `data/{owner}_{repo}.json`:

```json
[
  {
    "date": "2025-11-25",
    "total": 60149,
    "formats": [
      { "ext": ".exe", "count": 39200 },
      { "ext": ".asar", "count": 13007 },
      { "ext": ".dmg", "count": 4007 },
      { "ext": ".zip", "count": 2293 },
      { "ext": ".sig", "count": 1122 },
      { "ext": ".appimage", "count": 520 }
    ]
  }
]
```

**Группировка по форматам** вместо отдельных файлов делает данные компактными — 6 форматов вместо 80+ отдельных файлов.

## Особенности

- **Нет зависимостей**: Только Node.js встроенные модули (`node:https`, `node:fs`, `node:path`)
- **ESM**: Современный синтаксис import/export
- **Автоматизация**: GitHub Actions с cron-расписанием (ежедневно в 00:00 UTC)
- **Премиальный дизайн**: Градиенты, glow-эффект, анимации появления (draw animation)
- **Полная пагинация**: Автоматическая обработка репозиториев с любым количеством релизов (100 релизов на страницу)
- **Компактное хранение**: Группировка по форматам файлов вместо отдельных ассетов
- **Traffic clones**: При первом запуске попытка получить данные о клонах за последние 14 дней (требуется admin доступ)
- **Работает без токена**: GitHub API без токена (60 req/h) или с токеном (5000 req/h)

## Пример использования

Этот репозиторий отслеживает скачивания [SoundCloud-Desktop](https://github.com/loli669/SoundCloud-Desktop):
- **60K+ общих скачиваний**
- **62 релиза**
- Топ-формат: `.exe` (39K скачиваний)

## Лицензия

MIT
